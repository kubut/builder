<!DOCTYPE html>
<html>
<head>
    <link href="http://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.8/css/materialize.min.css">
    <link rel="stylesheet" href="{{ asset('bundles/builder/css/style.css') }}"/>
    <title>Builder - logowanie</title>
</head>

<body>

<div class="container">
    <div class="row">
        <div id="login-box" class="center-box col s12 m4">
            <div class="card">
                <div class="card-content white-text blue darken-2">
                    <span class="card-title">Logowanie</span>
                </div>
                <div class="card-content ">
                    <form action="{{ path("fos_user_security_check") }}" method="post">
                        {% if csrf_token %}
                            <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                        {% endif %}
                        <div class="row no-margin-bottom ">
                            <div class="input-field col s12 m12 l12">
                                <input id="username" name="_username" type="email" class="validate">
                                <label for="username">Adres e-mail</label>
                            </div>
                            <div class="input-field col s12 m12 l12">
                                <input id="password" name="_password" type="password" class="validate">
                                <label for="password">Hasło</label>
                                <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
                            </div>
                        </div>
                        {% if error %}
                            <div class="error-box">
                                <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                            </div>
                        {% endif %}
                        <div class="row no-margin-bottom ">
                            <div class="col s12 m12 l12">
                                <button type="submit" id="_submit" class="right waves-effect waves-light btn blue darken-2">Loguj
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<script type="text/javascript" src="https://code.jquery.com/jquery-2.1.1.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.8/js/materialize.min.js"></script>
</body>
</html>